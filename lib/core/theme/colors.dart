import 'package:flutter/material.dart';

const surfaceColor = Color.fromARGB(255, 4, 0, 75);

const primaryColor = Color.fromARGB(255, 10, 0, 194);

const onSurface = Color.fromARGB(255, 2, 0, 47);

const primaryContainerColor = Colors.white;
